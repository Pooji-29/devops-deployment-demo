{
  "name": "devops-deployment-demo",
  "version": "1.0.0",
  "description": "My first DevOps project",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo 'Test passed' && node -e \"require('http').get('http://localhost:3000/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})\" || true"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
